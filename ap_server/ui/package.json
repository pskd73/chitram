{
  "name": "chitram-ap-ui",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build:css && npm run embed",
    "build:css": "npx @tailwindcss/cli -i ./input.css -o ./dist/style.css --minify",
    "embed": "node ./embed.mjs"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.11",
    "daisyui": "^5.0.46",
    "tailwindcss": "^4.1.11"
  }
}
